---
title: 算法笔记
published: 2026-03-05
updated: 2026-03-19
description: 算法设计学习
tags: [Python]
category: Technology
author: minai
draft: false
---

# 算法基础

## 引言

这个页面用来记录本人从零开始的算法学习过程和一些可能用到的笔记. (非科班出身所以接触到的内容比较浅显)



## 查找算法

### 二分查找

最简单的查找算法, 面对有序列表, 每次从列表中间查找, 最多需要 $log_2{n}$ 次查找即可完成, 而简单查找至多需要 $n$ 步

示例(Python):

```python
def binary_search(arr, item):
    low = 0
    high = len(arr) - 1

    while low <= high:
        mid = (low + high) // 2
        guess = arr[mid]
        if guess == item:
            return mid
        elif guess > item:
            high = mid - 1
        else:
            low = mid + 1

    return -1
```



## 排序算法

### 冒泡排序

最容易实现的排序算法, 原理是通过不断比较相邻元素大小, 将较小的元素提至列表前而将较大的元素提至列表后

时间复杂度:

- 最坏: $O(n^2)$ , 当整个列表逆序时

- 最好: $O(n)$ , 当列表已经有序时

- 平均: $O(n^2)$

空间复杂度: 

- $O(1)$ , 由于是原地排序算法, 不需要额外存储空间

示例(C):

```c
void bubbleSort(int *arr, size_t len)
{
	int temp;
	for (int i = 0; i < len - 1; i++) {
		for (int j = 0; j < len - i - 1; j++) {
			if (arr[j] > arr[j + 1]) {
				temp = arr[j];
				arr[j] = arr[j + 1];
				arr[j + 1] = temp;
			}
		}
	}
}
```

示例(Python):

```python
def bubble_sort(arr):
    for i in range(len(arr)):
        for j in range(0, len(arr) - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]

    return arr
```













